import { Meta } from "@storybook/addon-docs";
import Text from "./core/Text";
import Background from "./core/Background";
import Card from "./core/Card";
import app from "../package.json";
import { getTheme } from "./theme";

<Meta title="Welcome/Library-UI" />

<Background style={{ width: "100%", height: "100%" }}>
  <Card
    style={{
      padding: 40,
      position: "absolute",
      margin: "auto",
      top: 0,
      bottom: 0,
      left: 0,
      right: 0,
      width: "fit-content",
      height: "fit-content",
      flexDirection: "column",
    }}
  >
    <div
      style={{
        width: 440,
        height: 200,
        backgroundImage: `url("./logo.svg")`,
        backgroundSize: "contain",
        backgroundPosition: "center",
        backgroundRepeat: "no-repeat",
      }}
    />
    <Text
      size={4}
      weight="bolder"
      style={{
        color: getTheme().colors.theme2,
      }}
      children="React components"
    />
    <Text
      style={{
        color: getTheme().colors.theme2,
        marginBottom: 10,
      }}
      children="for faster, cleaner web development"
    />
    <Text children="A customizable library built with accessibility and performance in mind." />
    <Text children="Start with Material Design or craft your own unique design system." />
  </Card>
</Background>
